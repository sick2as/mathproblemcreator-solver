import random
num1,num2 = random.randint(1,9), random.randint(0,9)
#next udate adds more than just addition but I just wanted to make this real simple
'''
#only use if using linux command line to run
@mpine.lip():
 asy nc def return
  as root {path}
'''
print(f"the sum of {num1} and {num2} is {int(num1) + int(num2)}")
